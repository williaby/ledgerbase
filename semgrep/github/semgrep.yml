# name = semgrep.yml
# description = Semgrep configuration for auditing GitHub Actions, secrets, and workflow misconfigurations.
# category = ci
# usage = Used by GitHub Actions workflows to enforce CI pipeline security and correctness.
# behavior = Audits GitHub workflow YAML files for misconfigurations, exposed secrets, and unsafe patterns.
# inputs = .github/workflows/*.yml
# outputs = CI job logs, optional SARIF report via CLI
# dependencies = Semgrep CLI
# author = Byron Williams
# last_modified = 2025-04-17
# tags = github, workflows, ci, security
# changelog = Added structured metadata and applied GitHub-related rulesets.

rules:
  - ruleset: r/github-actions
  - ruleset: r/semgrep-misconfigurations
  - ruleset: r/secrets
